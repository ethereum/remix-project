name: PRs reviews reminder

on:
  schedule:
    - cron: "0 8-17/7 * * 1-5"
  workflow_dispatch:

jobs:
  pr-reviews-reminder:
    runs-on: ubuntu-latest
    steps:
    - uses: yann300/pr-reviews-reminder-action@master
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        webhook-url: 'https://discord.com/api/webhooks/1183697720578416751/lpivNULL2_q1kUGA1yBxQURuvp6I29m47RTMs-UepVMSQKT9MvLi10MAqQCjxY6zWNCF/github' # Required
