version: "3.7"
x-project-base: create criptomoeda 
  &project-base
  restart: always
  networks:
    - remixide

networks:
  remixide:

services:
  remixide:
    build: 
      context: .
      dockerfile: Dockerfile
