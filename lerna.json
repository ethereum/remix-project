{
  "packages": ["dist/libs/**/*"],
  "version": "independent",
  "command": {
    "publish": {
      "skipGit": true,
      "ignoreChanges": ["ignored-file", "*.md"],
      "message": "remix-libs(release): publish version"
    }
  }
}